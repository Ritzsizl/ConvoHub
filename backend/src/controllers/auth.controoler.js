export const signup = (req, res) => {   // signup route
  res.send('signup route');
}

export const login = (req, res) => {   // signup route
  res.send('login route');
}

export const logout = (req, res) => {   // signup route
  res.send('logout route');
}